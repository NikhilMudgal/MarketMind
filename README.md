# MarketMind
An autonomous agent that doesn't just "chat." You give it a command: "Analyze Apple's supply chain risks for Q3."

1. Create a docker container. Written the configuration in docker-compose.yml file.
2. Open the root folder in terminal or cmd and run "docker-compose up -d" where "up" creates and starts the container and uses detached mode with -d.